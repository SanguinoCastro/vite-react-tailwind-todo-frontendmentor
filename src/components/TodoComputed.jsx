const TodoComputed = () => {
    return (
        <section className="flex justify-between rounded-b-md bg-white p-4">
            <span className="text-gray-400">5 items pendientes</span>
            <button className="text-gray-400">Limpiar completadas</button>
        </section>
    );
};

export default TodoComputed;
